-- Database initialization script for Render
-- This will be executed when the PostgreSQL container starts

-- Create database if not exists (this is handled by environment variables)
-- CREATE DATABASE IF NOT EXISTS healapp;

-- Create user if not exists (this is handled by environment variables)
-- CREATE USER IF NOT EXISTS healapp_user WITH ENCRYPTED PASSWORD 'healapp_password';

-- Grant privileges (this is handled by environment variables)
-- GRANT ALL PRIVILEGES ON DATABASE healapp TO healapp_user;

-- Enable extensions that might be needed (PostgreSQL only)
-- Uncomment the next line if using PostgreSQL
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- You can add more initialization SQL here if needed
